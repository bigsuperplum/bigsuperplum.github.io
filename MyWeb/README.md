# MyWeb

这是一个简单的个人网页示例，包含主页 `index.html`（位于主文件夹）和若干子页面（位于 `web/`），图片放在 `image/` 文件夹中。

打开方式（PowerShell 示例）：

```powershell
Start-Process "C:\Users\pig15\Desktop\MyWeb\index.html"
```

所有页面使用相对路径引用图片与样式，满足移植性要求。
